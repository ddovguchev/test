# direnv configuration for NixOS flake development
# Install direnv: nix profile install nixpkgs#direnv
# Then run: direnv allow

use flake

# Optional: Show flake info
if ! nix flake check &>/dev/null; then
  echo "⚠️  Warning: Flake check failed. Run 'nix flake check' for details."
fi
